# 试卷管理系统 - 打包与分发指南

## 快速打包（推荐）

### 方法一：使用打包脚本

1. 双击运行 `打包.bat`
2. 等待打包完成
3. 在 `dist` 目录找到打包好的程序

### 方法二：打包成EXE

1. 双击运行 `打包EXE.bat`
2. 等待PyInstaller打包完成
3. 在 `dist\试卷管理系统` 目录找到可执行文件

## 打包产物说明

### 方式一：完整目录包

```
试卷管理系统/
├── 启动系统.bat          # 双击启动
├── 安装依赖.bat          # 安装Python依赖
├── 使用说明.txt          # 使用说明
├── app.py               # 主程序
├── database.py          # 数据库模块
├── ai_analyzer.py       # AI模块
├── cache.py             # 缓存模块
├── requirements.txt     # 依赖列表
├── templates/           # 模板文件
├── data/                # 数据库目录
├── uploads/             # 上传目录
└── logs/                # 日志目录
```

**使用方法：**
1. 在目标机器安装Python 3.8+
2. 解压程序目录
3. 双击 `安装依赖.bat`
4. 双击 `启动系统.bat`

### 方式二：EXE可执行文件

```
试卷管理系统/
├── 试卷管理系统.exe      # 主程序
├── 启动系统.bat          # 启动脚本
├── _internal/           # 依赖文件
├── data/                # 数据库目录
├── uploads/             # 上传目录
└── logs/                # 日志目录
```

**使用方法：**
1. 解压程序目录
2. 双击 `启动系统.bat` 或 `试卷管理系统.exe`
3. 浏览器访问 http://localhost:5001

## 分发方式

### 方式一：ZIP压缩包

1. 运行打包脚本
2. 将 `dist\试卷管理系统` 目录压缩成ZIP
3. 发送ZIP文件给用户
4. 用户解压后即可使用

### 方式二：U盘拷贝

1. 运行打包脚本
2. 将 `dist\试卷管理系统` 目录复制到U盘
3. 在目标机器运行

### 方式三：网盘分享

1. 运行打包脚本
2. 上传ZIP到网盘
3. 分享下载链接

## 注意事项

### 系统要求

- Windows 10/11
- Python 3.8+（完整目录包需要）
- 2GB+ 磁盘空间
- 网络连接（首次使用AI功能时）

### 数据备份

重要数据存储在 `data\exam_system.db` 文件中，建议定期备份。

### 端口冲突

如果端口5001被占用，修改 `app.py` 最后一行：

```python
app.run(host='0.0.0.0', port=5002, debug=True)  # 改成其他端口
```

### 防火墙设置

如果需要局域网访问，需要在防火墙中允许Python或程序通过。

## 常见问题

**Q: 打包后运行报错？**
A: 确保所有依赖已安装，运行 `pip install -r requirements.txt`

**Q: EXE文件太大？**
A: PyInstaller会打包Python解释器和所有依赖，这是正常的

**Q: 如何减小包体积？**
A: 使用虚拟环境，只安装必要的包

**Q: 杀毒软件报毒？**
A: 这是误报，可以添加信任或使用完整目录包

## 高级打包选项

### 使用虚拟环境打包（推荐）

```bash
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
pip install pyinstaller

# 打包
pyinstaller 试卷管理系统.spec
```

### 使用Docker打包

```dockerfile
FROM python:3.12-slim

WORKDIR /app
COPY . .
RUN pip install -r requirements.txt

EXPOSE 5001
CMD ["python", "app.py"]
```

## 技术支持

如有问题，请查看：
- 开发文档.md
- README.md
- 提交Issue
