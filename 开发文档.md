# 试卷管理系统 - 开发文档 v1.8

## 一、项目概览

- **路径**: `C:\Users\jqlbx\Desktop\试卷管理系统\`
- **Python**: `C:\Users\jqlbx\AppData\Local\Programs\Python\Python312\python.exe`
- **端口**: 5001
- **默认管理员**: admin / admin123
- **总代码量**: ~7,400行（Python 3,791行 + HTML 3,572行）
- **数据库**: SQLite (`data/exam_system.db`)

### 核心功能
1. 试卷管理（上传/编辑/预览/搜索）
2. AI智能分析（5种策略，7种AI provider）
3. 错题本（自动/手动添加，掌握度追踪）
4. 专项练习（AI出题 + 高考真题匹配）
5. 改错管理（多次改错跟踪）
6. 高考真题库（AI自动分类，树形浏览）
7. 学习统计（成绩趋势图表）

---

## 二、文件结构

```
试卷管理系统/
├── app.py                 # 主程序（Flask路由）     1507行
├── database.py            # 数据库模块（CRUD）       880行
├── ai_analyzer.py         # AI分析模块（7种provider） 469行
├── exam_recognizer.py     # 试卷识别（OCR+关键词）   704行
├── storage.py             # 存储模块（本地/云端）     231行
├── requirements.txt       # 依赖列表
├── data/
│   └── exam_system.db     # SQLite数据库
├── uploads/               # 上传文件存储目录
└── templates/
    ├── base.html          # 基础模板（导航栏）
    ├── login.html         # 登录页
    ├── dashboard.html     # 首页仪表盘
    ├── exam_list.html     # 试卷列表（含预览）
    ├── exam_detail.html   # 试卷详情（AI分析+编辑+错题提取）
    ├── upload_exam.html   # 上传试卷
    ├── analysis_list.html # 分析管理
    ├── mistake_list.html  # 错题本
    ├── practice_list.html # 练习列表
    ├── do_practice.html   # 做练习
    ├── practice_result.html # 练习结果（含改错）
    ├── correction_list.html # 改错管理
    ├── gaokao_list.html   # 高考真题库（分类树）
    ├── gaokao_import.html # 导入真题（文件上传）
    ├── study_stats.html   # 学习统计
    ├── settings.html      # 系统设置
    └── user_list.html     # 用户管理
```

---

## 三、数据库表结构（12张表）

### 1. users - 用户表
```sql
id            INTEGER PK
username      TEXT NOT NULL UNIQUE
password      TEXT NOT NULL          -- werkzeug哈希
role          TEXT DEFAULT 'student' -- admin/student
display_name  TEXT
grade         TEXT
class_name    TEXT
created_at    TEXT DEFAULT CURRENT_TIMESTAMP
```

### 2. exams - 试卷表
```sql
id            INTEGER PK
title         TEXT NOT NULL
subject       TEXT NOT NULL
grade         TEXT
exam_type     TEXT DEFAULT '月考'    -- 月考/期中/期末/模拟/高考真题/竞赛
exam_date     TEXT
total_score   REAL DEFAULT 150
description   TEXT
file_path     TEXT                   -- 本地文件路径
file_type     TEXT                   -- jpg/pdf/docx等
cloud_url     TEXT                   -- 云端URL
upload_user_id INTEGER
created_at    TEXT DEFAULT CURRENT_TIMESTAMP
```

### 3. exam_files - 试卷文件表
```sql
id            INTEGER PK
exam_id       INTEGER NOT NULL -> exams(id) CASCADE
file_name     TEXT NOT NULL
file_path     TEXT
cloud_url     TEXT
file_type     TEXT
page_number   INTEGER DEFAULT 1
created_at    TEXT DEFAULT CURRENT_TIMESTAMP
```

### 4. questions - 题目表
```sql
id              INTEGER PK
exam_id         INTEGER NOT NULL -> exams(id) CASCADE
question_number INTEGER
question_type   TEXT DEFAULT '选择题'
content         TEXT
answer          TEXT
score           REAL DEFAULT 0
user_answer     TEXT
user_score      REAL
created_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

### 5. ai_analysis - AI分析结果表
```sql
id              INTEGER PK
exam_id         INTEGER NOT NULL -> exams(id) CASCADE
analysis_type   TEXT DEFAULT '综合分析'  -- 综合分析/知识点总结/学习建议/错题提取与分析
content         TEXT
model_name      TEXT
difficulty      TEXT               -- 从AI结果提取
knowledge_summary TEXT             -- 从AI结果提取
question_count  INTEGER            -- 从AI结果提取
created_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

### 6. mistake_book - 错题本表
```sql
id              INTEGER PK
user_id         INTEGER NOT NULL -> users(id)
question_id     INTEGER
exam_id         INTEGER
subject         TEXT
question_type   TEXT
content         TEXT
correct_answer  TEXT
user_answer     TEXT
analysis        TEXT
mastery_level   INTEGER DEFAULT 0  -- 0-5掌握度
review_count    INTEGER DEFAULT 0
next_review_date TEXT
created_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

### 7. study_stats - 学习统计表
```sql
id              INTEGER PK
user_id         INTEGER NOT NULL -> users(id)
subject         TEXT
exam_date       TEXT
score           REAL
total_score     REAL
rank            INTEGER
class_avg       REAL
created_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

### 8. config - 系统配置表
```sql
id              INTEGER PK
config_key      TEXT NOT NULL UNIQUE
config_value    TEXT
description     TEXT
updated_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

### 9. practice_sessions - 练习会话表
```sql
id              INTEGER PK
user_id         INTEGER NOT NULL -> users(id)
mistake_id      INTEGER -> mistake_book(id)
subject         TEXT
practice_type   TEXT DEFAULT '错题巩固'
title           TEXT
total_questions INTEGER DEFAULT 0
correct_count   INTEGER DEFAULT 0
status          TEXT DEFAULT '进行中' -- 进行中/已完成
score           REAL
started_at      TEXT DEFAULT CURRENT_TIMESTAMP
completed_at    TEXT
```

### 10. practice_questions - 练习题目表
```sql
id              INTEGER PK
session_id      INTEGER NOT NULL -> practice_sessions(id) CASCADE
question_number INTEGER
question_type   TEXT DEFAULT '解答题'
content         TEXT
options         TEXT               -- 选择题选项（换行分隔）
correct_answer  TEXT
user_answer     TEXT
is_correct      INTEGER DEFAULT 0
analysis        TEXT
created_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

### 11. correction_records - 改错记录表
```sql
id                  INTEGER PK
user_id             INTEGER NOT NULL -> users(id)
mistake_id          INTEGER -> mistake_book(id)
practice_question_id INTEGER -> practice_questions(id)
session_id          INTEGER -> practice_sessions(id)
original_mistake    TEXT
user_correction     TEXT
is_correct          INTEGER DEFAULT 0
teacher_comment     TEXT
correction_count    INTEGER DEFAULT 1
created_at          TEXT DEFAULT CURRENT_TIMESTAMP
```

### 12. gaokao_questions - 高考真题库表
```sql
id              INTEGER PK
subject         TEXT NOT NULL
category        TEXT               -- 知识模块分类（三角函数/力学等）
year            INTEGER
region          TEXT               -- 全国甲卷/北京卷/新高考I等
question_number INTEGER
question_type   TEXT DEFAULT '选择题'
content         TEXT NOT NULL
options         TEXT
correct_answer  TEXT
analysis        TEXT
knowledge_point TEXT               -- 逗号分隔
difficulty      TEXT DEFAULT '中等' -- 简单/中等/困难
score           REAL
source          TEXT               -- 来源试卷名称
created_at      TEXT DEFAULT CURRENT_TIMESTAMP
```

---

## 四、Flask路由清单

### 认证
| 方法 | 路由 | 功能 |
|------|------|------|
| GET/POST | `/login` | 登录 |
| GET | `/logout` | 退出 |
| GET | `/` | 重定向到dashboard |

### 试卷管理
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/dashboard` | 首页仪表盘 |
| GET | `/exams` | 试卷列表（支持搜索筛选） |
| GET/POST | `/exams/upload` | 上传试卷 |
| GET | `/exams/<id>` | 试卷详情 |
| POST | `/exams/<id>/edit` | 编辑试卷信息 |
| POST | `/exams/<id>/delete` | 删除试卷 |
| GET | `/api/exams/<id>/files` | 获取试卷文件列表API |

### AI分析
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/exams/<id>/analyses/check` | 检查已有分析 |
| POST | `/exams/<id>/analyze` | AI分析试卷 |
| POST | `/exams/<id>/chat` | AI对话问答 |
| GET | `/analyses` | 分析管理列表 |
| POST | `/analyses/<id>/delete` | 删除分析记录 |

### 错题本
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/mistakes` | 错题本列表 |
| POST | `/mistakes/add` | 添加错题 |
| POST | `/mistakes/<id>/update` | 更新错题 |

### 专项练习
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/practice` | 练习列表 |
| POST | `/practice/start/<mistake_id>` | 从错题开始练习（AI出题+高考真题匹配） |
| GET | `/practice/<session_id>` | 做练习页面 |
| POST | `/practice/<session_id>/submit` | 提交答案 |
| GET | `/practice/<session_id>/result` | 练习结果 |
| POST | `/practice/<session_id>/correction` | 提交改错 |
| GET | `/corrections` | 改错管理 |

### 高考真题
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/gaokao` | 真题库（分类树浏览，多维筛选） |
| POST | `/gaokao/add` | 添加真题 |
| POST | `/gaokao/<id>/edit` | 编辑真题 |
| POST | `/gaokao/<id>/delete` | 删除真题 |
| POST | `/gaokao/batch-classify` | AI批量自动分类 |
| GET/POST | `/gaokao/import` | 上传文件导入（doc/docx/pdf/txt） |
| GET | `/api/gaokao/search` | 搜索真题API |

### 学习统计
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/stats` | 学习统计页面 |
| POST | `/stats/add` | 添加成绩 |

### 系统设置
| 方法 | 路由 | 功能 |
|------|------|------|
| GET | `/settings` | 设置页面 |
| POST | `/settings/update` | 更新配置 |
| POST | `/settings/test-ai` | 测试AI连接 |
| GET | `/users` | 用户管理 |
| POST | `/users/add` | 添加用户 |
| GET | `/uploads/<path>` | 静态文件服务 |

---

## 五、AI模块架构

### AIAnalyzer类 (`ai_analyzer.py`)

```python
class AIAnalyzer:
    def __init__(self, provider, api_key, model, base_url)
    
    # 分析试卷（5种策略）
    def analyze_exam(exam_info, questions, image_paths, analysis_type, exam_text)
    
    # 通用对话
    def chat(message, context)
    
    # 生成练习题
    def generate_practice_questions(mistake_info, num_questions)
    
    # Provider调用
    def _call_openai_compatible(prompt, image_paths)  # OpenAI/Moonshot
    def _call_claude(prompt, image_paths)
    def _call_qwen(prompt, image_paths)
    def _call_zhipu(prompt, image_paths)
    def _call_local(prompt, image_paths)               # Ollama
```

### 支持的AI Provider
| Provider | Base URL | 支持Vision | 备注 |
|----------|----------|-----------|------|
| openai | api.openai.com/v1 | ✅ | GPT-4 |
| deepseek | api.deepseek.com/v1 | ❌ | 不支持image_url |
| claude | api.anthropic.com | ✅ | 需特殊header |
| qwen | dashscope.aliyuncs.com | ❌ | 阿里通义 |
| zhipu | open.bigmodel.cn | ❌ | 智谱GLM |
| moonshot | api.moonshot.cn/v1 | ✅ | Kimi |
| local | localhost:11434 | ❌ | Ollama |

### 分析策略
1. **综合分析** - 难度评估、知识点分布、题型分析
2. **知识点总结** - 核心知识点、考察频率
3. **学习建议** - 学习计划、提升方案
4. **错题分析** - 错误原因、解题思路
5. **错题提取与分析** - AI识别错题并自动存入错题本（返回JSON）

### get_analyzer()工厂函数
从数据库config表读取provider/api_key/model/base_url，创建AIAnalyzer实例。

---

## 六、关键实现细节

### 1. 试卷识别流程 (exam_recognizer.py)
```
文件上传 → 判断类型 → OCR提取文字 → 基础识别（关键词） → AI识别（兜底）
图片: pytesseract OCR
PDF: PyMuPDF提取文字 → PyPDF2 → OCR回退
Word: python-docx提取
```

### 2. PDF转图片 (app.py: convert_pdf_to_images)
```python
import fitz
doc = fitz.open(pdf_path)
for page in doc:
    pix = page.get_pixmap(matrix=fitz.Matrix(2.0, 2.0))
    img_data = base64.b64encode(pix.tobytes("png")).decode()
    result['images'].append(f'data:image/png;base64,{img_data}')
```

### 3. 高考真题分类系统
- 导入时AI自动识别：科目、年份、地区、分类（知识模块）
- AI批量分类：对未分类题目按学科知识模块归类
- 树形浏览：科目 → 分类 → 题型
- 分类参考：每个学科有预设的知识模块列表

### 4. 错题练习流程
```
错题本 → 开始练习 → AI生成3道模拟题 + 搜索2道匹配高考真题
→ 做练习 → 提交判分 → 练习结果 → 改错练习 → 改错记录
```

### 5. 数据库迁移模式
```python
def _migrate_db():
    migrations = [
        ("table", "column", "TYPE"),
        ...
    ]
    for table, column, col_type in migrations:
        try:
            c.execute(f'ALTER TABLE {table} ADD COLUMN {column} {col_type}')
        except sqlite3.OperationalError:
            pass  # 列已存在
```

---

## 七、已知问题和解决方案

### 1. DeepSeek不支持Vision
DeepSeek API不接受`image_url`消息类型，返回400错误。
**解决**: ai_analyzer.py中DeepSeek走纯文字分析路径，image_paths=None。

### 2. data:image URI不能当文件路径
convert_pdf_to_images返回的base64 data URI被open()当作文件路径。
**解决**: ai_analyzer.py增加`elif img_path.startswith('data:')`分支直接传给API。

### 3. Jinja2变量名session覆盖Flask的session
render_template(session=xxx)会覆盖Flask的session对象。
**解决**: 变量名改为practice_session。**永远不要用session作为模板变量名**。

### 4. Windows路径双反斜杠
SQLite存储路径格式为`uploads\\exams\\xxx.pdf`。
**解决**: `path.replace('\\\\', os.sep).replace('\\', os.sep)`

### 5. Tesseract路径必须显式设置
```python
pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
```

### 6. Python完整路径
用户系统python命令不工作，需用完整路径：
`C:\Users\jqlbx\AppData\Local\Programs\Python\Python312\python.exe`

### 7. AI返回JSON需清理
AI返回可能包含markdown代码块标记：
```python
json_str = content.strip()
if json_str.startswith('```'):
    json_str = json_str.split('\n', 1)[1] if '\n' in json_str else json_str[3:]
if json_str.endswith('```'):
    json_str = json_str[:-3]
if json_str.startswith('json'):
    json_str = json_str[4:]
data = json.loads(json_str.strip())
```

### 8. Flask大函数编辑风险
analyze_exam等100+行函数进行patch时容易破坏try/except/if-else嵌套结构。
**建议**: 对大函数进行结构性修改时，重写整个函数。

### 9. PDF文字必须传给AI
pdf_text提取后未传给analyzer导致不支持vision的provider看不到内容。
**解决**: analyze_exam()新增exam_text参数，所有prompt附加试卷文字。

---

## 八、依赖清单

### 必需
```
flask>=2.0.0
requests>=2.28.0
werkzeug>=2.0.0
```

### 文件处理
```
PyMuPDF (fitz)       # PDF处理
PyPDF2               # PDF文字提取
python-docx          # Word文档处理
pillow               # 图片处理
pytesseract          # OCR文字识别
```

### 系统依赖
```
Tesseract OCR v5.4.0  # C:\Program Files\Tesseract-OCR
chi_sim.traineddata   # 中文语言包
```

### 可选
```
oss2                  # 阿里云OSS
qcloud-cos-python-sdk # 腾讯云COS
qiniu                 # 七牛云
minio                 # MinIO
```

---

## 九、配置项（config表）

| Key | 说明 | 示例 |
|-----|------|------|
| ai_provider | AI提供商 | deepseek |
| ai_api_key | API密钥 | sk-xxx |
| ai_model | 模型名 | deepseek-chat |
| ai_base_url | 自定义URL | https://api.deepseek.com/v1 |
| cloud_provider | 云存储类型 | 阿里云OSS |
| cloud_endpoint | 云存储端点 | oss-cn-hangzhou.aliyuncs.com |
| cloud_access_key | AccessKey | - |
| cloud_secret_key | SecretKey | - |
| cloud_bucket | Bucket名 | - |
| upload_max_size | 最大上传(MB) | 50 |
| allowed_extensions | 允许的文件类型 | jpg,jpeg,png,gif,bmp,pdf,doc,docx |

---

## 十、启动方式

```bash
cd C:\Users\jqlbx\Desktop\试卷管理系统
C:\Users\jqlbx\AppData\Local\Programs\Python\Python312\python.exe app.py
# 访问 http://localhost:5001
```

或使用批处理文件启动。

---

## 十一、v1.9 更新内容

### UI美化
1. **深色模式**: 支持深色/浅色主题切换，自动保存到localStorage
2. **侧边栏折叠**: 可折叠侧边栏，节省屏幕空间
3. **页面加载动画**: 页面加载时显示旋转动画
4. **仪表盘图表**: 使用Chart.js显示成绩趋势和科目分布
5. **响应式设计**: 优化移动端显示效果

### 功能增强
1. **错题复习提醒**: 
   - 基于艾宾浩斯遗忘曲线的复习算法
   - 支持按优先级分类（急需/今日/即将到期）
   - 一键标记已复习，自动计算下次复习日期

2. **学习计划**:
   - 创建和管理学习计划
   - 任务清单和进度跟踪
   - 学习日历和今日任务
   - 学习建议和统计

3. **导出PDF报告**:
   - 生成完整的学习报告
   - 包含统计数据、各科成绩、错题分析
   - 支持打印和导出PDF

4. **批量导入试卷**:
   - 支持拖拽上传多个文件
   - 自动识别试卷信息
   - 上传进度显示

5. **高考真题智能分类** (v1.9新增):
   - 完整的9科知识点分类体系
   - AI自动识别知识模块、具体知识点、难度
   - 支持按知识点、难度、题型、年份筛选
   - 关键词搜索（题目内容、知识点、解析）
   - 批量AI分类（同时更新分类、知识点、难度）
   - 新增gaokao_classifier.py分类模块

### 性能优化
1. **分页功能**: 试卷列表支持分页，每页20条
2. **缓存机制**: 
   - 内存缓存减少数据库查询
   - 支持缓存过期和自动清理
   - 数据更新时自动清除相关缓存

3. **数据库索引**:
   - 为常用查询字段创建索引
   - 自动更新数据库统计信息
   - 提高查询性能

---

## 十二、新增文件说明

```
试卷管理系统/
├── cache.py               # 缓存模块
├── gaokao_classifier.py   # 高考真题智能分类模块
└── templates/
    ├── macros/
    │   └── pagination.html # 分页宏
    ├── review_reminder.html # 复习提醒页面
    ├── study_plan.html     # 学习计划页面
    ├── study_report.html   # 学习报告页面
    └── batch_import.html   # 批量导入页面
```

---

## 十三、新增路由

| 路由 | 方法 | 功能 |
|------|------|------|
| `/review-reminder` | GET | 复习提醒页面 |
| `/mistakes/<id>/review` | POST | 标记已复习 |
| `/study-plan` | GET | 学习计划页面 |
| `/plans/add` | POST | 添加学习计划 |
| `/plans/<id>/complete` | POST | 完成计划 |
| `/plans/<id>/delete` | POST | 删除计划 |
| `/plans/<id>/tasks/<id>/toggle` | POST | 切换任务状态 |
| `/stats/report` | GET | 学习报告 |
| `/exams/batch-import` | GET | 批量导入页面 |

---

## 十四、后续可扩展方向

1. **题库扩充**: 接入更多高考真题数据源，支持OCR扫描件批量导入
2. **智能推荐**: 基于错题历史和掌握度，AI推荐个性化练习
3. **知识图谱**: 构建学科知识点关联图，可视化薄弱环节
4. **多人协作**: 支持教师端出题、学生端作答、自动批改
5. **移动端**: 微信小程序版本，支持拍照搜题
6. **数据分析**: 学习行为分析、成绩预测模型
