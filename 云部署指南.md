# 试卷管理系统 - 云部署指南

## 方案一：云服务器部署（推荐）

### 1. 购买云服务器

推荐配置：
- 阿里云/腾讯云/AWS
- 2核4G内存
- 40GB硬盘
- Ubuntu 22.04系统

### 2. 连接服务器

```bash
ssh root@你的服务器IP
```

### 3. 一键部署脚本

```bash
# 下载并运行部署脚本
wget https://raw.githubusercontent.com/your-repo/deploy.sh
chmod +x deploy.sh
./deploy.sh
```

或者手动部署：

```bash
# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装Python
apt install python3 python3-pip python3-venv -y

# 3. 安装Nginx
apt install nginx -y

# 4. 创建项目目录
mkdir -p /var/www/exam-system
cd /var/www/exam-system

# 5. 上传项目文件（使用scp或git）
scp -r /path/to/试卷管理系统/* root@服务器IP:/var/www/exam-system/

# 6. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 7. 安装依赖
pip install -r requirements.txt
pip install gunicorn

# 8. 初始化数据库
python -c "import database; database.init_db()"

# 9. 配置Gunicorn
cat > gunicorn.conf.py << EOF
bind = "127.0.0.1:5001"
workers = 4
timeout = 120
EOF

# 10. 创建系统服务
cat > /etc/systemd/exam-system.service << EOF
[Unit]
Description=Exam System
After=network.target

[Service]
User=www-data
WorkingDirectory=/var/www/exam-system
Environment="PATH=/var/www/exam-system/venv/bin"
ExecStart=/var/www/exam-system/venv/bin/gunicorn -c gunicorn.conf.py app:app
Restart=always

[Install]
WantedBy=multi-user.target
EOF

# 11. 启动服务
systemctl daemon-reload
systemctl enable exam-system
systemctl start exam-system

# 12. 配置Nginx
cat > /etc/nginx/sites-available/exam-system << EOF
server {
    listen 80;
    server_name your-domain.com;  # 或服务器IP

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host \$host;
        proxy_set_header X-Real-IP \$remote_addr;
        proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
    }

    location /uploads {
        alias /var/www/exam-system/uploads;
    }
}
EOF

ln -s /etc/nginx/sites-available/exam-system /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

---

## 方案二：Docker部署（简单）

### 1. 创建Dockerfile

```dockerfile
FROM python:3.12-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    tesseract-ocr \
    tesseract-ocr-chi-sim \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install gunicorn

# 复制项目文件
COPY . .

# 创建数据目录
RUN mkdir -p data uploads/exams logs

# 暴露端口
EXPOSE 5001

# 启动命令
CMD ["gunicorn", "-b", "0.0.0.0:5001", "-w", "4", "--timeout", "120", "app:app"]
```

### 2. 创建docker-compose.yml

```yaml
version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: always
```

### 3. 构建并运行

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f
```

---

## 方案三：Heroku部署（免费额度）

### 1. 准备文件

创建 `Procfile`:
```
web: gunicorn app:app
```

创建 `runtime.txt`:
```
python-3.12.0
```

### 2. 部署步骤

```bash
# 安装Heroku CLI
curl https://cli-assets.heroku.com/install.sh | sh

# 登录
heroku login

# 创建应用
heroku create exam-system-app

# 部署
git init
git add .
git commit -m "Initial commit"
git push heroku main

# 初始化数据库
heroku run python -c "import database; database.init_db()"

# 打开应用
heroku open
```

---

## 方案四：Railway部署（推荐新手）

### 1. 准备项目

确保项目根目录有：
- `requirements.txt`
- `Procfile` (内容: `web: gunicorn app:app`)

### 2. 部署步骤

1. 访问 https://railway.app
2. 使用GitHub登录
3. 点击 "New Project" -> "Deploy from GitHub repo"
4. 选择你的仓库
5. 在 Variables / 环境变量中添加：
   - `AI_PROVIDER=deepseek`
   - `AI_API_KEY=你的DeepSeek API Key`
   - `AI_MODEL=deepseek-v4-flash`
   - `AI_BASE_URL=https://api.deepseek.com`
6. 自动部署完成

添加以上变量后，云端启动时会自动把 DeepSeek 写入系统配置；打开网页即可直接测试 AI 识别/分析，不需要再进设置页手动填写。

---

## 方案五：腾讯云Serverless部署

### 1. 安装Serverless Framework

```bash
npm install -g serverless
```

### 2. 创建serverless.yml

```yaml
component: flask
name: exam-system
app: exam-system-app

inputs:
  src:
    src: ./
    exclude:
      - .env
      - .git
      - .gitignore
      - data/**
      - uploads/**
  region: ap-guangzhou
  runtime: Python3.12
  handler: app.app
  events:
    - apigw:
        parameters:
          protocols:
            - http
            - https
          endpoints:
            - path: /
              method: ANY
```

### 3. 部署

```bash
serverless deploy
```

---

## 域名和HTTPS配置

### 1. 购买域名

- 阿里云万网
- 腾讯云DNSPod
- Cloudflare

### 2. 配置DNS

添加A记录，指向服务器IP

### 3. 配置HTTPS（免费SSL证书）

```bash
# 安装Certbot
apt install certbot python3-certbot-nginx -y

# 获取证书
certbot --nginx -d your-domain.com

# 自动续期
certbot renew --dry-run
```

---

## 数据备份策略

### 1. 自动备份脚本

创建 `backup.sh`:
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/var/backups/exam-system"
mkdir -p $BACKUP_DIR

# 备份数据库
cp /var/www/exam-system/data/exam_system.db $BACKUP_DIR/exam_$DATE.db

# 备份上传文件
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz /var/www/exam-system/uploads/

# 保留最近30天的备份
find $BACKUP_DIR -mtime +30 -delete
```

### 2. 设置定时任务

```bash
# 每天凌晨3点备份
crontab -e
0 3 * * * /var/www/exam-system/backup.sh
```

---

## 监控和日志

### 1. 查看服务状态

```bash
systemctl status exam-system
```

### 2. 查看日志

```bash
# 应用日志
journalctl -u exam-system -f

# Nginx日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### 3. 监控脚本

创建 `monitor.sh`:
```bash
#!/bin/bash
if ! curl -s http://localhost:5001 > /dev/null; then
    systemctl restart exam-system
    echo "$(date): Service restarted" >> /var/log/exam-system-monitor.log
fi
```

---

## 常见问题

### Q: 端口被占用？
```bash
lsof -i:5001
kill -9 PID
```

### Q: 权限问题？
```bash
chown -R www-data:www-data /var/www/exam-system
chmod -R 755 /var/www/exam-system
```

### Q: 数据库锁定？
```bash
systemctl restart exam-system
```

### Q: 内存不足？
```bash
# 减少worker数量
# 编辑 gunicorn.conf.py
workers = 2
```

---

## 推荐方案

| 方案 | 难度 | 成本 | 推荐度 |
|------|------|------|--------|
| 云服务器 | 中 | ¥50-100/月 | ⭐⭐⭐⭐⭐ |
| Docker | 中 | ¥50-100/月 | ⭐⭐⭐⭐ |
| Heroku | 低 | 免费/¥50/月 | ⭐⭐⭐ |
| Railway | 低 | 免费/¥30/月 | ⭐⭐⭐⭐ |
| Serverless | 中 | 按量付费 | ⭐⭐⭐ |

**新手推荐**: Railway 或 Heroku
**生产环境**: 云服务器 + Docker
