# 快速部署指南

## 已创建的部署文件

```
试卷管理系统/
├── Dockerfile           # Docker镜像配置
├── docker-compose.yml   # Docker Compose配置
├── Procfile             # Heroku/Railway配置
├── runtime.txt          # Python版本配置
├── .gitignore           # Git忽略文件
├── deploy.sh            # 一键部署脚本
├── backup.sh            # 数据备份脚本
├── monitor.sh           # 服务监控脚本
└── 云部署指南.md         # 详细部署文档
```

---

## 方案一：Railway部署（最简单，推荐新手）

### 步骤：

1. **注册Railway账号**
   - 访问 https://railway.app
   - 使用GitHub账号登录

2. **创建GitHub仓库**
   ```bash
   cd C:\Users\jqlbx\Desktop\试卷管理系统
   git init
   git add .
   git commit -m "Initial commit"
   # 推送到你的GitHub仓库
   ```

3. **部署到Railway**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"
   - 选择你的仓库
   - 自动部署完成！

4. **访问应用**
   - Railway会提供一个域名，如 `exam-system.up.railway.app`

---

## 方案二：Heroku部署（免费额度）

### 步骤：

1. **安装Heroku CLI**
   - 下载：https://devcenter.heroku.com/articles/heroku-cli

2. **登录并部署**
   ```bash
   heroku login
   heroku create exam-system-app
   git push heroku main
   heroku run python -c "import database; database.init_db()"
   heroku open
   ```

---

## 方案三：Docker部署（推荐生产环境）

### 步骤：

1. **安装Docker**
   - Windows：下载Docker Desktop
   - Linux：`curl -fsSL https://get.docker.com | sh`

2. **构建并运行**
   ```bash
   cd C:\Users\jqlbx\Desktop\试卷管理系统
   docker-compose up -d
   ```

3. **访问应用**
   - 打开 http://localhost:5001

---

## 方案四：云服务器部署（最灵活）

### 步骤：

1. **购买云服务器**
   - 阿里云：https://www.aliyun.com
   - 腾讯云：https://cloud.tencent.com
   - 推荐：2核4G，Ubuntu 22.04

2. **连接服务器**
   ```bash
   ssh root@服务器IP
   ```

3. **运行部署脚本**
   ```bash
   # 上传项目文件到服务器
   scp -r ./* root@服务器IP:/var/www/exam-system/
   
   # 运行部署脚本
   chmod +x deploy.sh
   ./deploy.sh
   ```

---

## 部署后配置

### 1. 配置域名（可选）

- 购买域名（阿里云万网、腾讯云DNSPod）
- 添加DNS解析，指向服务器IP
- 配置HTTPS证书

### 2. 配置HTTPS

```bash
# 安装Certbot
apt install certbot python3-certbot-nginx -y

# 获取证书
certbot --nginx -d your-domain.com
```

### 3. 设置自动备份

```bash
# 编辑定时任务
crontab -e

# 添加以下内容（每天凌晨3点备份）
0 3 * * * /var/www/exam-system/backup.sh

# 添加监控（每5分钟检查）
*/5 * * * * /var/www/exam-system/monitor.sh
```

---

## 常用命令

### 服务管理
```bash
# 查看状态
systemctl status exam-system

# 启动/停止/重启
systemctl start exam-system
systemctl stop exam-system
systemctl restart exam-system

# 查看日志
journalctl -u exam-system -f
```

### Docker管理
```bash
# 查看容器
docker ps

# 查看日志
docker logs exam-system

# 进入容器
docker exec -it exam-system bash

# 停止/启动
docker-compose stop
docker-compose start
```

---

## 推荐方案

| 场景 | 推荐方案 | 说明 |
|------|----------|------|
| 新手学习 | Railway | 最简单，一键部署 |
| 个人使用 | Heroku | 有免费额度 |
| 小团队 | 云服务器 | 灵活，可定制 |
| 生产环境 | Docker + 云服务器 | 稳定，易维护 |

---

## 注意事项

1. **数据备份**：定期备份 `data/exam_system.db` 文件
2. **安全配置**：修改默认密码，配置防火墙
3. **性能优化**：根据访问量调整worker数量
4. **日志监控**：定期查看日志，及时发现问题

---

## 获取帮助

- 查看详细文档：`云部署指南.md`
- 查看项目文档：`开发文档.md`
- 提交Issue反馈问题
